/**
 * 
 * Relation: \
 * A, B, C →synthesizes→ S \
 * S = synthesis node
 * 
 * 
 * Meaning: \
 * Combine multiple nodes into one coherent idea, summary, or structure. \
 * It does NOT add new information, it reorganizes what already exists.
 */

export const synthesizesPrompt = ``;