/**
 * 
 * Relation: \
 * A → (background) → B \
 * A = context / definitions / prior work \
 * B = idea / problem / analysis 
 * 
 * 
 * Meaning: \
 * Node A contains the knowledge you need before reasoning inside B.
 * 
 */

export const backgroundPrompt = ``;