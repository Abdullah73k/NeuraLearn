/**
 * Relation: \
 * A → (challenges) → B \
 * A = objection / counterexample \
 * B = claim / conclusion
 * 
 * 
 * Meaning: \
 * Node A complicates, contradicts, or weakens the claim in B.
 * 
 */

export const challengesPrompt = ``;